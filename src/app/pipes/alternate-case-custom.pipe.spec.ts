import { AlternateCaseCustomPipe } from './alternate-case-custom.pipe';

describe('AlternateCaseCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new AlternateCaseCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
